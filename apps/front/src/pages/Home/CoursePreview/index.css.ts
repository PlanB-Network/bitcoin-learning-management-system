import { style } from '@vanilla-extract/css';

export const coursePreviewCard = style({ margin: '20px 40px', width: '400px' });

export const coursePreviewImg = style({
  width: '100%',
});

export const coursePreviewTag = style({
  marginLeft: 10,
});

export const coursePreviewFooter = style({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
});
