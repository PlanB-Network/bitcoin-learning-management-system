{
  "dependencies": {
    "@blms/constants": "workspace:*",
    "@blms/database": "workspace:*",
    "@blms/opentimestamps": "workspace:*",
    "@blms/s3": "workspace:*",
    "@blms/service-common": "workspace:*",
    "@pdf-lib/fontkit": "^1.1.1",
    "@trpc/server": "^11.9.0",
    "@types/qr-image": "^3.2.9",
    "argon2": "^0.44.0",
    "bip39": "^3.1.0",
    "pdf-lib": "^1.17.1",
    "qr-image": "^3.2.0",
    "sharp": "^0.34.5",
    "stripe": "^19.3.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@blms/types": "workspace:*"
  },
  "exports": {
    "development": "./src/index.ts",
    "import": "./dist/src/index.js",
    "types": "./src/index.ts"
  },
  "imports": {
    "#src/*": "./src/*"
  },
  "main": "./src/index.ts",
  "name": "@blms/service-user",
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "build-types": "tsc --emitDeclarationOnly -p tsconfig.json",
    "check-types": "tsc --noEmit",
    "clear": "rm -rf ./dist && rm -f *.tsbuildinfo",
    "lint": "biome check .",
    "postbuild": "cp -r src/lib/pdf dist/src/lib/",
    "trace-performance": "npx tsc --extendedDiagnostics --noEmit -p tsconfig.json | head -n 12 > performance.log",
    "type-check": "tsc --build",
    "watch-types": "tsc --emitDeclarationOnly -p tsconfig.json --watch --preserveWatchOutput"
  },
  "type": "module",
  "types": "./src/index.ts",
  "version": "0.0.1"
}
